<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="80dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="10dp"
    android:background="@drawable/shape_item">
    <ImageView
        android:id="@+id/ivCurrency"
        android:layout_width="40dp"
        android:layout_height="40dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:src="@mipmap/ic_launcher"/>
    <TextView
        android:id="@+id/tvCurrencyName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toRightOf="@id/ivCurrency"
        app:layout_constraintBottom_toBottomOf="@id/ivCurrency"
        app:layout_constraintTop_toTopOf="@id/ivCurrency"
        android:layout_marginLeft="20dp"
        android:textSize="15sp"
        tools:text="ETH"/>
    <TextView
        android:id="@+id/tvValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toTopOf="@id/tvFormatValue"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="200.0 ETH"/>
    <TextView
        android:id="@+id/tvFormatValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintRight_toRightOf="@id/tvValue"
        app:layout_constraintTop_toBottomOf="@id/tvValue"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text="$ 100000"/>

</androidx.constraintlayout.widget.ConstraintLayout>